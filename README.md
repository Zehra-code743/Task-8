# 📘 Simple Calculator (CLI + Streamlit)

This project implements a clean and modular **Simple Calculator** that works in:

- **Command-Line Interface (CLI)**
- **Streamlit Web UI**

It uses **Speckit Plus** for structured planning and implementation.

## 🚀 Project Workflow

Below is the exact step-by-step method used to generate this project using Speckit Plus, Claude, and your dual-terminal workflow.

### Create Project Folder

Create a folder named:

Simple Calculator

### Open Command Prompt in the Folder

Right-click → **Open in Terminal**  
OR open CMD and navigate to the folder.

### 3. Initialize Speckit Plus Project

```bash
specifyplus init calculator

### 4. Navigate Into the Project Folder

```bash
cd calculator

### 5. Open Two CMD Windows

**Terminal 1: Start Speckit Continuous Runtime**
```bash
ccr start
Terminal 2: Open Claude Code Workspace

bash
Copy code
cc code

### 6. Run Speckit Plus Stages in Claude

In Claude, run the sections one by one:

/sp.constitution
/sp.specify
/sp.plan
/sp.tasks
/sp.implement

### 7. Files Auto-Generated by Claude

- `calculator.py` — math functions  
- `utils.py` — input validation  
- `cli_main.py` — CLI version  
- `app.py` — Streamlit UI

### 8. Run CLI Calculator

```bash
python cli_main.py

### 9. Run Streamlit Web UI

```bash
streamlit run app.py

## Project Summary

This project demonstrates how to use **Speckit Plus + Python** to create a modular calculator with:

- Separate calculation logic  
- Shared validation layer  
- Two interface modes (CLI and UI)  
- Clean and scalable architecture

## Reflection (Concise)

The project successfully implements a stable, modular calculator in both CLI and Streamlit modes. Validation prevents crashes, and shared logic ensures consistency. The UI remains simple and effective. Future improvements could include history logs, more operations, a better UI theme, and automated testing.

